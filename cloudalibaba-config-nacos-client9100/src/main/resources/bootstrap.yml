server:
  port: 9100

spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.0.200:8848 #Nacos服务注册中心地址（本机的写localhost）
      config:
        server-addr: 192.168.0.200:8848 #Nacos作为配置中心地址（本机的写localhost）
        file-extension: yaml #指定yml格式配置
